let n = 12345;
    const b = n.toString();
    const c = b.split('');
    const d = c.reverse();
    
    const f = (n) => {for(let i =0; i<d.length; i++ ){
        const arr = [];
        arr.push(Number(d[i]))}
        return arr;
    }

    console.log(f(n));
    
